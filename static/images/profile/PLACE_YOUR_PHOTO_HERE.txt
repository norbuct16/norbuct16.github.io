Put your profile/portrait photo in this folder, named exactly:

    profile.jpg

Full expected path: static/images/profile/profile.jpg

Once the file is there, it appears on the site automatically — no code
changes needed. Delete this text file whenever you like; it's just a marker.

Suggested: a square-ish or portrait-oriented photo (roughly 4:5). It will be
cropped to fit that shape automatically, so a slightly wider/taller photo
is fine.
